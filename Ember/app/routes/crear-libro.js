// app/routes/crear-libro.js
import Route from '@ember/routing/route';

export default class CrearLibroRoute extends Route {
  model() {
    // Aquí puedes pasar cualquier dato que necesites al template, si es necesario
  }
}
